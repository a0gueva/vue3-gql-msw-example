{
  "name": "vue3-graphql-msw-app-final",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run msw:init && vite",
    "build": "npm run msw:init && vite build",
    "serve": "serve -s dist -l 4173",
    "msw:init": "msw init public/ --save",
    "test": "vitest"
  },
  "dependencies": {
    "vue": "^3.3.0",
    "@apollo/client": "^3.7.0",
    "@vue/apollo-composable": "^4.0.0",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "msw": "^1.2.1",
    "vite": "^4.0.0",
    "vitest": "^0.34.0",
    "serve": "^14.2.0"
  }
}
